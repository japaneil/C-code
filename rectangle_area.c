#include <stdio.h>

void main()
{
    float length, breath;
    printf("enter the length of the rectangle - ");
    scanf("%f", &length);
    printf("enter the breadth of the rectangle - ");
    scanf("%f", &breath);
    printf("the area of the reactangle is %f unit square", length*breath);

}

